{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/autowarefoundation/autoware-index/main/schema/distribution.schema.json",
  "title": "Autoware Index distribution file",
  "description": "Schema for distributions/<distro>.yaml in the autoware-index registry. schema_version 2: repositories are first-class. Each entry owns one URL and ONE ref, and nests the registered packages it hosts (lockstep: every package of a repository is validated and distributed at that single ref; per-package ref skew is unrepresentable). Cross-entry invariants the schema cannot express (globally unique package names per distro, canonical-URL uniqueness across repository entries) are enforced by scripts/check_refs.py; tag-vocabulary membership (schema/tags.yaml) by scripts/check_tags.py.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "ros_distro", "repositories"],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "2"
    },
    "ros_distro": {
      "type": "string",
      "description": "ROS 2 distribution name. MUST equal the filename stem (e.g. 'jazzy' for distributions/jazzy.yaml).",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "repositories": {
      "type": "object",
      "description": "Registered repositories keyed by repository name (the registry-wide identity used in sweep artifacts, history records, and CLI .repos entries).",
      "additionalProperties": { "$ref": "#/$defs/repository" },
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9_-]*$"
      }
    }
  },
  "$defs": {
    "repository": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url", "ref", "governance", "maintainers", "packages"],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "minLength": 1,
          "description": "Git remote URL of the repository. One entry per repository; spelling variants of the same repo (.git suffix, ssh form, trailing slash) are rejected by check_refs.py. (format is annotation-only in most validators, hence the explicit minLength: an empty url would otherwise pass CI and be silently skipped by every sweep.)"
        },
        "ref": { "$ref": "#/$defs/ref" },
        "governance": {
          "type": "string",
          "enum": ["community", "foundation"]
        },
        "reference_design": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "enum": ["pov"] },
          "uniqueItems": true,
          "description": "Optional, reviewer-granted: the named AWF reference designs whose published documentation cites this repository ('pov' = the privately-owned-vehicles design). The grant test is that citation, so any reviewer can confirm or refuse a value from public documents. The enum grows one value per design whose owners confirm its spelling; it is display and filter metadata, never a ref selector, and stays outside the sweep diff tuple."
        },
        "maintainers": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/maintainer" },
          "description": "Repository-level default maintainers; a package entry may override with its own list."
        },
        "packages": {
          "type": "object",
          "minProperties": 1,
          "description": "Registered ROS packages this repository hosts, keyed by ROS package name (always explicit: the key must equal the package.xml <name>). Unlisted sibling packages in the same repository are not registered: the index makes no claims about them.",
          "additionalProperties": { "$ref": "#/$defs/package" },
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          }
        }
      }
    },
    "package": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tags"],
      "properties": {
        "tags": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9_-]*$" },
          "uniqueItems": true,
          "description": "One or more tags from the closed vocabulary in schema/tags.yaml. This schema checks shape only; membership and deprecation are enforced by scripts/check_tags.py."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Optional registry-side override for the card description. When set it wins over the package.xml <description> cached by the sweep; omit it to render the upstream description."
        },
        "maintainers": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/maintainer" },
          "description": "Optional override of the repository-level maintainers for this package."
        }
      }
    },
    "maintainer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "email", "github"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "email": { "type": "string", "format": "email" },
        "github": { "type": "string", "minLength": 1 }
      }
    },
    "ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "value"],
      "description": "What this repository is registered as for this distro. Single ref per (repository, distro): every registered package of the repository is validated at this one ref.",
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["tag", "sha", "branch"],
          "description": "tag/sha = pinned (immutable cells in history). branch = rolling (re-resolved by nightly sweeps)."
        },
        "value": {
          "type": "string",
          "minLength": 1
        }
      }
    }
  }
}
